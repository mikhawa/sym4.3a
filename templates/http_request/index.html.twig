{% extends 'base.html.twig' %}

{% block title %}Les requêtes http!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Les requêtes http ✅</h1>

    <ul>
        <li>Lien avec 1 variable get numérique au hasard:<br> <a href="{{ path("voir_un_id",{'id':id}) }}">{{ path("voir_un_id",{'id':id}) }}</a></li>
        {% set hasardTwig = random(1,100) %}
        <li>Lien avec 1 variable get numérique au hasard et une variable get "test":<br> <a href="{{ path("voir_un_id",{'id':id}) }}?test={{ hasardTwig }}">{{ path("voir_un_id",{'id':id}) }}?test={{ hasardTwig }}</a></li>
    </ul>
</div>
{% endblock %}
